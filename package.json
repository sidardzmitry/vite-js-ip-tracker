{
  "name": "ip-tracker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.0.12"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "gh-pages": "^6.1.1",
    "leaflet": "^1.9.4"
  }
}
